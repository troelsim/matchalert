<h2>New Player subscription</h2>

<%= render "form.html", changeset: @changeset,
                        action: player_subscription_path(@conn, :create) %>

<span><%= link "Back", to: player_subscription_path(@conn, :index) %></span>
