<h2>New Subscription</h2>

<%= render "form.html", changeset: @changeset,
                        action: subscription_path(@conn, :create) %>

<span><%= link "Back", to: subscription_path(@conn, :index) %></span>
